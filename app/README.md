# Shared Clipboard

Vite + Tauri 2 + React 19 + TypeScript + Tailwind CSS 4.

## Develop

```bash
npm install
npm run tauri dev
```

Frontend-only preview (Tauri commands fall back to a browser message):

```bash
npm run dev
```

## Build

```bash
npm run tauri build
```
